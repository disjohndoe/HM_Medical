---
date: 2026-04-21
topic: errors
status: active
---

# 2.2 Ponavljajući (hr-create-health-issue-recurrence-message) rejected with ERR_HEALTH_ISSUE_2000

## Discovery

Wired up end-to-end 2.2 flow (FE "Ponovi slučaj" action on resolved cases →
backend dispatch → Certilia signing → CEZIH POST). CEZIH signed the bundle
successfully but the state machine rejected the POST with:

```
HTTP 400
{
  "code": "ERR_HEALTH_ISSUE_2000",
  "display": "Unexpected error occurred while executing state machine perform.",
  "diagnostics": "FHIR error: FHIR issue code: not-supported"
}
```

Tested on prod (app.hmdigital.hr) via Certilia against parent case:
- Parent: Zatvoren J20 "Akutni bronhitis" (case_id `cmo5rif7001wthb85f3i74hiq`)
- Patient: GORAN PACPRIVATNICI19 (MBO 999990260)

## Evidence

Signed bundle sent to `health-issue-services/api/v1/$process-message`:

```json
{
  "resourceType": "Bundle",
  "type": "message",
  "entry": [
    {
      "resource": {
        "resourceType": "MessageHeader",
        "eventCoding": {
          "system": "http://ent.hr/fhir/CodeSystem/ehe-message-types",
          "code": "2.2"
        },
        "sender": {"identifier": {"system": "...HZZO-sifra-zdravstvene-organizacije", "value": "999001464"}},
        "author": {"identifier": {"system": "...HZJZ-broj-zdravstvenog-djelatnika", "value": "7659059"}},
        "source": {"endpoint": "urn:oid:1.2.162.1.999001464"},
        "focus": [{"reference": "urn:uuid:<Condition-fullUrl>"}]
      }
    },
    {
      "resource": {
        "resourceType": "Condition",
        "verificationStatus": {"coding": [{"system": "...condition-ver-status", "code": "confirmed"}]},
        "code": {"coding": [{"system": "...icd10-hr", "code": "J20", "display": "Akutni bronhitis"}]},
        "subject": {"identifier": {"system": "...MBO", "value": "999990260"}},
        "onsetDateTime": "2026-04-21T07:11:27.151614+02:00",
        "asserter": {"identifier": {"system": "...HZJZ-broj-zdravstvenog-djelatnika", "value": "7659059"}}
      }
    }
  ],
  "signature": { ... JWS detached, same format as 2.1/2.4 which work ... }
}
```

Bundle matches the documented profile contract in
`backend/app/services/cezih/fhir_api/condition.py:181-214`:
> Profile: hr-create-health-issue-recurrence-message|0.1
> Requires: ICD code + verificationStatus + onset[x], identifier FORBIDDEN
> (server assigns new global identifier).

Identifier IS stripped after build (`condition.pop("identifier", None)`).
Extsigner signing succeeds (same JWS format that 2.1/2.4/2.9 use). Bundle
validates locally.

Pre-flight GET `Condition?_count=0` on `health-issue-services` returns 400
`Missing mandatory parameter "patient"` (documented non-fatal warning, same
as for all 2.x paths). Session cookie warms and the POST proceeds.

## Impact

- TC17b Ponavljajući (2.2) — **cannot ship**. State machine rejects signed
  bundle. Not a blocker for certification (2.2 is not in the 22 TCs), but
  is a documented feature gap.
- Backend code path (FE → dispatch → service → builder → FHIR client) is
  otherwise healthy. Fix is likely narrow — a missing or mis-shaped field
  in the `hr-create-health-issue-recurrence-message` profile.
- FE action "Ponovi slučaj" was deployed briefly and reverted in the same
  commit chain. Left the backend dispatcher local-DB parent lookup
  (replaces flaky `real_service.retrieve_cases` qedm call) since that's a
  real improvement.

## H1 mirror (drop asserter) — FAILED 2026-04-21

Applied the 2.6 fix (drop `asserter`) to `create_recurring_case` in
`backend/app/services/cezih/fhir_api/condition.py` (commit `6b29da4`).
After deploy, retriggered 2.2 via devtools fetch against the same J20
Zatvoren case (`cmo5rif7001wthb85f3i74hiq`), same patient.

Bundle confirmed to NOT contain asserter:
```
"resource": {"resourceType": "Condition",
  "verificationStatus": {...},
  "code": {"coding": [{..., "code": "J20"}]},
  "subject": {...}, "onsetDateTime": "2026-04-21T07:45:04..."}
```

CEZIH still rejected:
```
HTTP 400 in 2869.5ms
MessageHeader.response.code = fatal-error
OperationOutcome.issue[0].code = not-supported
details.coding[0] = ERR_HEALTH_ISSUE_2000
```

**Conclusion:** asserter is NOT the 2.2 root cause. 2.6 fix was
event-specific. Need a different hypothesis for 2.2.

## Hypotheses to investigate (not yet tried)

1. **MessageHeader.eventCoding.system mismatch** — bundle uses
   `http://ent.hr/fhir/CodeSystem/ehe-message-types`, but other CEZIH-verified
   events (2.1/2.4/2.9) also use this same system and are accepted. So
   probably not this.
2. **Missing `Condition.clinicalStatus`** — 2.2 profile may want
   `clinicalStatus=recurrence` (analogous to 2.4 requiring `resolved`).
   Profile doc says only "identifier forbidden, ICD+verification+onset
   required"; clinicalStatus may be implicitly required.
3. **Missing link to parent Condition** — a recurrence that CEZIH can't
   trace to a prior resolved Condition may get rejected. `Condition.evidence`
   or `Condition.extension` with parent case identifier may be needed.
4. **`asserter` vs `recorder`** — profile may slice differently for
   recurrences vs new cases.
5. **`encounter` reference required** — per `case-lifecycle-profile-matrix.md`
   some profiles want an active Encounter at the time of the message.

## Action Items

- [x] Keep backend dispatcher local-DB parent lookup (commit f6c6690)
- [x] Revert FE "Ponovi slučaj" exposure until profile is fixed
- [x] Pulled `hr-create-health-issue-recurrence-message|0.1` SD — our payload
      is profile-legitimate (identifier forbidden ✓, clinicalStatus forbidden
      ✓, abatement forbidden ✓, asserter allowed mustSupport)
- [x] H1 mirror (drop asserter) — FAILED (commit `6b29da4`). Asserter is
      NOT the 2.2 root cause, unlike 2.6.
- [ ] **Next hypothesis (H2): add `vezani-slucaj` extension** — `hr-condition`
      base profile defines this extension (min=0, max=1, on Condition) with
      sub-extensions `Veza` (Reference to parent hr-condition) + `TipVeze`
      (CodeableConcept). Not mandated by the recurrence-message profile, but
      plausibly what the state machine consults to identify the prior
      resolved case.
- [ ] If H2 fails, try H3: add `clinicalStatus=recurrence` (the 2.6
      update ValueSet includes it — may implicitly apply to 2.2 too).
- [ ] Once working, re-enable `create_recurring` in `CASE_ACTIONS`
