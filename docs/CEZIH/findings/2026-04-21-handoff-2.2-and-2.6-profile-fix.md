---
date: 2026-04-21
topic: errors
status: partial — 2.6 resolved, 2.2 still pending
---

# HANDOFF — Fix CEZIH 2.2 Ponavljajući + 2.6 Data update (ERR_HEALTH_ISSUE_2000)

> **Update 2026-04-21 (after H1 test):** 2.6 RESOLVED by dropping
> `asserter` (commit `5cb984c`). 2.2 Ponavljajući still pending — next
> step is to apply the same delta to `build_condition_create` and
> retest via backend POST (FE button currently hidden).

Self-contained brief for a fresh chat. Do NOT read the whole session — this
doc has everything needed to pick up and debug these two blockers end-to-end
in the browser.

## TL;DR

Two CEZIH case-lifecycle events, **2.2** (Ponavljajući / recurrence) and
**2.6** (data update), both fail against real CEZIH test env with the same
generic state-machine rejection:

```
HTTP 400
OperationOutcome.issue[0].code = not-supported
details = ERR_HEALTH_ISSUE_2000
display = "Unexpected error occurred while executing state machine perform."
```

Certilia signing succeeds. Bundle reaches CEZIH. CEZIH state machine rejects.
No `diagnostics` text — CEZIH does not say what's wrong. Must diff our payload
against the Simplifier `StructureDefinition` and narrow by experiment.

**Both are TC17 certification scope. Must be fixed before on-site exam.**

## What's working (don't break these)

Verified live on prod today against real CEZIH using **Certilia mobile** signing,
test patient GORAN PACPRIVATNICI19 (UUID `245ec690-a024-4c90-b6d3-62621eca4d47`,
MBO `999990260`):

| Event | Action | Code path | Status |
|-------|--------|-----------|--------|
| 2.1 | Create | `build_condition_create` → `create_case` | ✅ |
| 2.3 | Remisija | `build_condition_status_update(cs=remission)` | ✅ |
| 2.4 | Resolve | `build_condition_status_update(cs=resolved, abatement)` | ✅ |
| 2.5 | Relapse | `build_condition_status_update(cs=None)` | ✅ |
| 2.9 | Reopen | `build_condition_status_update(cs=None)` | ✅ |

The builder for working lifecycle ops (`build_condition_status_update` at
`backend/app/services/cezih/builders/condition.py:87`) emits a MINIMAL Condition:
`identifier` (ID_CASE_GLOBAL), `subject`, optional `clinicalStatus`, optional
`abatementDateTime`. **That's it.**

## What's failing

### 2.2 Ponavljajući (recurrence)
- Builder: `build_condition_create` → `create_recurring_case` (condition/fhir_api/condition.py:176)
- Identifier IS stripped after build (`condition.pop("identifier", None)`)
- Emits: verificationStatus, code (ICD), subject, onsetDateTime, asserter, (optional note)
- FE action reverted in commit `d1367c2` — button NOT exposed to doctors
- BE kept: local-DB parent lookup via `CezihCase` (replaces flaky QEDm retrieve)
- Finding: `docs/CEZIH/findings/2026-04-21-cezih-2.2-recurrence-not-supported.md`

### 2.6 Data update
- Builder: `build_condition_data_update` at condition.py:129
- Dispatcher: `dispatch_update_case_data` at dispatchers/cases.py:437
- Emits: identifier (ID_CASE_GLOBAL, verified correct), subject, clinicalStatus (current),
  verificationStatus, code (ICD), onsetDateTime, asserter, note
- Button IS exposed in FE ("Izmijeni podatke slučaja (2.6)") — leave it in place
- Finding: `docs/CEZIH/findings/2026-04-21-cezih-2.6-data-update-not-supported.md`

## Root hypothesis (same for both)

The delta between working and failing payloads is:

**Working (2.4/2.9)** sends: identifier + subject + clinicalStatus + abatementDateTime
**Failing (2.2/2.6)** adds: `code` (ICD), `onsetDateTime`, `asserter`, `verificationStatus`, `note`

One of those extra fields is not sliced-in on the 2.2/2.6 profile. Most likely
candidates (ranked):

1. **`asserter`** — status updates never emit it. CEZIH may forbid changing asserter.
2. **`code` (ICD)** — unchanged ICD may need to be omitted, not re-sent.
3. **`onsetDateTime`** — timezone suffix (`+02:00`) vs date-only.
4. **`clinicalStatus` CodeSystem** — we use `terminology.hl7.org/...`, CEZIH may want `fhir.cezih.hr/...`.
5. **`note.extension` annotation-type code** — we always send code=4; update may need different.

## Ground truth — the Simplifier profiles

```bash
# Download and unpack authoritative profiles
curl -sL https://packages.simplifier.net/cezih.hr.condition-management/0.2.1 -o /tmp/ccm.tgz
mkdir -p /tmp/ccm && tar xzf /tmp/ccm.tgz -C /tmp/ccm
ls /tmp/ccm/package/
# Look for:
#   StructureDefinition-hr-create-health-issue-recurrence-message.json  (2.2)
#   StructureDefinition-hr-update-health-issue-data-message.json        (2.6)
```

Diff our emitted Condition fields against the SD's `snapshot.element` list —
specifically the `min`/`max` cardinalities and any `must-support` / sliced paths.

## Code you'll touch

All paths from repo root `C:\Dev\HM DIGITAL\MEDICAL_MVP\`:

- `backend/app/services/cezih/builders/condition.py` — `build_condition_create` (L29)
  and `build_condition_data_update` (L129). This is where fields get added.
- `backend/app/services/cezih/fhir_api/condition.py` — `create_recurring_case` (L176)
  (builds + optionally strips identifier) and `update_case_data` (L270).
- `backend/app/services/cezih/builders/common.py` — shared constants:
  `ID_CASE_GLOBAL`, `CS_CONDITION_CLINICAL`, etc.

DO NOT change: `build_condition_status_update` or any of the successful
2.1/2.3/2.4/2.5/2.9 code paths.

## How to reproduce end-to-end in browser (Chrome DevTools MCP)

**Prod only.** No local testing. Flow per `CLAUDE.md` Mandatory Testing Workflow.

1. **Check deploy status** (new chat will inherit my last commit):
   ```bash
   gh run list --branch main --limit 3 --json status,conclusion,headSha,displayTitle
   ```

2. **Browser setup** — Chrome is already open, patient page persisted:
   ```
   https://app.hmdigital.hr/pacijenti/245ec690-a024-4c90-b6d3-62621eca4d47
   ```
   Use `mcp__chrome-devtools__list_pages` then `navigate_page type=reload` to
   start clean.

3. **Reproduce 2.6** (fastest):
   - Click tab "CEZIH" → subtab "Slučajevi"
   - Find row: **J11 Influenca, virus nije dokazan — Aktivan + Potvrđen**
     (`cezih_case_id=cmo788c4o020rhb85dnikeniv`). Any J11 Aktivan+Potvrđen row
     works if that specific case is gone; create a new one via "Novi slučaj"
     (ICD=J11) if needed.
   - Click "Izmijeni podatke slučaja (2.6)" on that row
   - Change only the note, click "Spremi izmjene"
   - Approve Certilia push on the test phone (Goran Pacprivatnici19 device)
   - Expected after fix: toast "Slučaj ažuriran" + row "Izmjena" column updates
   - Expected before fix: toast "Greška na CEZIH-u. Pokušajte ponovno..."

4. **Reproduce 2.2** (slower — needs a closed case):
   - Find a `Zatvoren` case row (many exist on this patient)
   - 2.2 doesn't have a button — trigger via backend directly:
     ```bash
     # from a FE devtools console (authenticated cookie):
     fetch('/api/cezih/cases/' + CASE_ID + '/recurring?patient_id=245ec690-a024-4c90-b6d3-62621eca4d47', {method:'POST'})
     ```
     OR temporarily re-add the `create_recurring` entry to `CASE_ACTIONS` in
     `frontend/src/components/cezih/case-management.tsx` (see commit `d1367c2`
     for what was removed; the hook `useCreateRecurringCase` is still wired).

5. **Capture logs** (after every attempt):
   ```bash
   ssh root@178.104.169.150 "cd /opt/medical-mvp && docker compose logs backend --since 5m 2>&1 | grep -B2 -A60 'ERR_HEALTH_ISSUE_2000\|Case POST bundle' | tail -200"
   ```

6. **DB state check** (for the J11 case):
   ```bash
   ssh root@178.104.169.150 "cd /opt/medical-mvp && docker compose exec -T db bash -c 'psql -U \$POSTGRES_USER -d \$POSTGRES_DB -c \"SELECT id, local_case_id, cezih_case_id, icd_code, clinical_status, verification_status, last_error_code FROM cezih_cases WHERE cezih_case_id = '\\''cmo788c4o020rhb85dnikeniv'\\'';\"'"
   ```

## Experiment plan

One hypothesis per commit. After each commit: push → wait for deploy → browser
retest → read logs → record result in the finding doc.

1. **H1: drop `asserter`** in `build_condition_data_update` (5-line change).
   **✅ VERIFIED 2026-04-21** (commit `5cb984c`) — CEZIH HTTP 200, DB updated.
   See `2026-04-21-cezih-2.6-data-update-not-supported.md` for evidence.
2. ~~If H1 fails → H2: drop `code` if it equals existing row's `icd_code`~~
   (unneeded — H1 resolved 2.6)
3. ~~H3: onsetDateTime date-only~~ (unneeded)
4. ~~H4: change clinicalStatus CodeSystem~~ (unneeded)
5. **Next:** apply the same delta to `build_condition_create` /
   `create_recurring_case` for 2.2 and retest. If 2.2 still fails after
   dropping asserter, investigate the `hr-condition` `vezani-slucaj`
   extension (parent-case linkage via `Veza` Reference + `TipVeze`
   CodeableConcept) — not in the recurrence-message profile but may be
   what the state machine looks for to identify the prior resolved case.

## Commits on this issue today

```
d1367c2 revert(cezih): remove Ponovi slučaj FE action — 2.2 fails CEZIH profile
f6c6690 feat(cezih): expose 2.2 Ponavljajući in case UI + local-DB parent lookup (partially reverted)
98184c6 fix(cezih): restore datetime import in cases dispatcher
5e8a51c fix(cezih): prevent self-deadlock in clear_cezih_error on retry-after-error
7dba2a0 refactor(cezih): simplify error UX — single "retry in a few minutes" toast
```

`d1367c2` is tip of `main`. Deploy succeeded.

## Credentials & URLs reminder

- Prod: `https://app.hmdigital.hr/` (logged in as Marko Kovačević / Admin)
- SSH: `ssh root@178.104.169.150`, deploy path `/opt/medical-mvp`
- Test doctor: MBO `500604936`, HZJZ `7659059`, institution `999001464`
- Test patient for all case work: GORAN PACPRIVATNICI19 (UUID `245ec690-a024-4c90-b6d3-62621eca4d47`, MBO `999990260`)
- Signing: Certilia mobile (extsigner) — approve push on test phone

## DO NOT

- Don't disable the 2.6 UI button — it's in cert scope.
- Don't re-expose the 2.2 UI button until 2.6 is fixed (same root cause suspected;
  fix 2.6 first as the controlled experiment, then re-enable 2.2).
- Don't touch `build_condition_status_update` or any 2.4/2.9 path — those work.
- Don't amend commits. One hypothesis = one commit = one deploy = one retest.
- Don't skip the Simplifier SD diff — it's the fastest way to narrow candidates
  before guessing.

## After fixing

- Update BOTH findings docs with the root cause and the fix commit.
- Re-enable `create_recurring` in `CASE_ACTIONS` at
  `frontend/src/components/cezih/case-management.tsx` (see commit `d1367c2`
  diff for the exact lines removed).
- Run TC17 full sweep: 2.1, 2.3, 2.4, 2.5, 2.9 (regression) + 2.2, 2.6 (new).
- Then resume: TC15 → TC18 → TC19 → TC20 → TC21 → TC22 → TC11 foreigner.
